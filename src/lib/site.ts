// Central brand defaults. Anything stored in the database (SiteSetting)
// overrides these at runtime, so the admin can change them without code.
export const SITE = {
  name: 'Modern Dentology',
  tagline: 'Premium Dental Care',
  leadDoctor: 'Dr. Abdul Basit',
  primaryColor: 'teal'
};
